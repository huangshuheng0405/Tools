# 可迭代对象

如果一个对象实现了`Symbo.iterator`方法，那么这个对象就是可迭代对象，可以使用`for of`循环来遍历它

常见的可迭代对象

- 数组
- 字符串
- Set
- Map
- Arguments

一个对象之所以“可迭代”，是因为遵循了**迭代协议**：

1. 有一个特殊的属性：键名为`Symbol.iterator`的方法
2. 调用这个方法时，会返回一个迭代器（iterator），这个迭代器有一个`next()`方法
3. 每次调用`next()`，都会返回一个包含`value`（当前值）和`done`（是否结束）的对象
